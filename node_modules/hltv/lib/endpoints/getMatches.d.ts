import { UpcomingMatch } from '../models/UpcomingMatch';
import { LiveMatch } from '../models/LiveMatch';
import { HLTVConfig } from '../config';
import { MatchEventType } from '../enums/MatchEventType';
import { MatchFilter } from '../enums/MatchFilter';
declare type GetMatchesArguments = {
    eventID?: number;
    eventType?: MatchEventType;
    filter?: MatchFilter;
};
export declare const getMatches: (config: HLTVConfig) => ({ eventID, eventType, filter }?: GetMatchesArguments) => Promise<(UpcomingMatch | LiveMatch)[]>;
export {};
